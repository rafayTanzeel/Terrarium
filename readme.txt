Most of Core Code is for the embedded device, the web ui can be executed from any computer

To run node server, cd to WebUI, then do: 
1)npm install
2)npm start
3)On any browser(Chrome Recommended), http://localhost:3042/
